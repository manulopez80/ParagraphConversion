# Google Test library is included into this project instead of downloading it directly in compilation due to problems with GOOGLETEST_VERSION label (see general CmakeLists.txt).

# Not tested in Linux, only in Windows due to lack of time (I avoided external libraries like boost unless in spite of the code optimization).  
